<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('../partials/head', {title: 'Perfil Usuario'}) %>
<body>
	<!-- Header -->
	<%- include('../partials/headerNavbar') %>
	<!-- /Header -->

	<!-- user profile -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Perfíl de usuario</h2>
			</div>
		</div>
    <% if (typeof locals.user != 'undefined') { %>

    <h3>¡Bienvenido <%= locals.user.email %>!</h3>
	<a href="/avatar/upload">
		<% if (locals.user.avatar != undefined) { %>
			<img src="/images/users/<%= locals.user.avatar %>" class="avatarImg" alt="Avatar de usuario">
		<% } else { %>
			<img src="/images/users/default-avatar.png" class="avatarImg" alt="Avatar de usuario">
			<% } %>
	</a>
	<% if (locals.user.role == 'admin') { %>
		<h2>Rol: Administrador</h2>
	<% } else { %>
		<h2>Rol: Usuario</h2>
	<% } %>
	<p>Fecha de registro: <%= new Date(locals.user.created_at).toLocaleString() %> </p>
	<p>Email: <%= locals.user.email %></p>

    <% } %>
    <!-- /user profile -->

	<!-- footer -->
	<%- include('../partials/footer') %>
	<!-- /footer -->

</body>	
</html>