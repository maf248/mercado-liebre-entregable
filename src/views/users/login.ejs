<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('../partials/head', {title: 'Iniciar Sesión'}) %>
<body>
	<!-- Header -->
	<%- include('../partials/headerNavbar') %>
	<!-- /Header -->

	<!-- login form -->
    <div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Inicio de sesión</h2>
			</div>
		</div>
		<div class="col-12">
			<form action="/users/login" method="POST">
				<div class="product-detail">
					<div class="col-12 col-md-6">
						<label for="user" class="form-label">Dirección de Email:</label>
						<input type="text" id="user" name="user" placeholder="Ingresa tu dirección de email aquí" class="form-input" <% if (typeof body != 'undefined') { %> value="<%= body.user %>"<% } %>>
						<% if (typeof errors != "undefined") {
							errors.forEach ( error => { 
								if (error.param == "user") { %>
									<p><%= error.msg %></p>
							<% } }) } %>
					</div>
                    
                    <div class="col-12 col-md-6">
						<label for="password" class="form-label">Contraseña:</label>
						<input type="password" id="password" name="password" placeholder="Ingresa tu contraseña aquí" class="form-input" <% if (typeof body != 'undefined') { %> value="<%= body.password %>"<% } %>>
						<% if (typeof errors != "undefined") {
							errors.forEach ( error => { 
								if (error.param == "password") { %>
									<p><%= error.msg %></p>
							<% } }) } %>
					</div>
					
					<div class="col-12">
						<button type="submit" class="buy-now-button">Iniciar sesión</button>
					</div>
				</div>
			</form>
		</div>

	</div>
    <!-- /login form -->

	<!-- footer -->
	<%- include('../partials/footer') %>
	<!-- /footer -->

</body>	
</html>